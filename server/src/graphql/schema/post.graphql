scalar Date

type Post {
  postId: ID!,
  userId: ID!
  content: String!,
  createdOn: Date!,
  displayname: String,
  likesCount: Int!,
  currentUserLike: String
}

type Query {
  getPost(
    postId: String!
  ): Post
  getUserPosts(
    userId: String!
  ): [Post]!
  getFeedPosts(
    lastPostId: String,
    lastCreatedOn: Date,
    limit: Int!
  ): [Post]!
}

type Mutation {
  createPost(
    content: String!,
  ): Post!
  deletePost(
    postId: String!
  ): Boolean!
  generatePosts: Boolean
}